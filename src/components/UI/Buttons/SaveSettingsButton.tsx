import classes from './SaveSettingsButton.module.css';

const SaveSettingsButton = () => {
  return <button className={classes['save-settings-button']}>Save</button>;
};

export default SaveSettingsButton;
